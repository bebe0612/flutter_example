name: todo_with_provider
description: A simple todo app with provider pattern.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.0
  provider: ^5.0.0
  table_calendar: ^3.0.0-nullsafety.1
  hive: ^2.0.2
  hive_flutter: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.0.1
  build_runner: ^1.12.2

flutter:

  uses-material-design: true
